{% extends "@styles/backend/content/index.html.twig" %}

{% form_theme form 'form/fields.html.twig' %}
{% block body %}
    <div class="card">
        <div class="card-header">{{sub_header |trans }}</div>
        <div class="card-block">{{ form_start(form) }}
        {{ form_errors(form) }}
        {{ form_widget(form) }}
        {{ form_end(form) }}
        </div>
    </div>
{% endblock %}